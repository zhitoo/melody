# A Mini Project For Work With PHP Attribute

## your controller must have #[controller] attribute

## you must define route with #[Route(path, method, name)] on top of your methods

## run project with:

```
cd public
php -S localhost:8888
```
